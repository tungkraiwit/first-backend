var express = require('express');
var app = express.Router();

// const sqlite3 = require('sqlite3');
// const db = new sqlite3.Database('./classicmodels.sqlite', sqlite3.OPEN_READONLY);

// db.all('UPDATE ', (err, rows) => {
//   res.send(rows);
// });
